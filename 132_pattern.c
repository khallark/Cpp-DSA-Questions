#include <stdio.h>
#include <stdbool.h>
bool find132pattern(int* nums, int numsSize) {
    
}
int main() {
    int arr[] = {3,1,2,4};
    int size = sizeof(arr) / sizeof(int);
    printf("%d", find132pattern(arr, size));
    return 0;
}